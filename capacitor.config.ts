import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'com.montrelvo.sparkworkshop',
  appName: 'Spark Workshop',
  webDir: 'dist',
  backgroundColor: '#f5f4ed',
};
export default config;
